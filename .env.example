# ================================================
# MICROSERVICE ENVIRONMENT CONFIGURATION
# ================================================
# IMPORTANT: Copy this file to .env and fill in the values
# In Replit: Use the Secrets tab to set these values securely
# Never commit actual credentials to version control!

# ================================================
# CRITICAL: JWT AUTHENTICATION - READ THIS FIRST!
# ================================================
# This MUST match the JWT_SECRET used by your parent ERP application
# Without this, authentication will fail with "invalid signature" errors
# 
# For development/testing: Use 'dev-secret-key-for-testing-only'
# For production: Use the same secure key as your parent ERP app
#
# Common errors if JWT_SECRET is wrong:
# - "JsonWebTokenError: invalid signature"
# - "Authentication failed"
# - "401 Unauthorized"
JWT_SECRET=your-jwt-secret-key-must-match-parent-app

# ================================================
# DATABASE CONFIGURATION
# ================================================
# PostgreSQL connection for this microservice's data
# In Replit: This is automatically provided when you create a PostgreSQL database
# Look for DATABASE_URL in your Replit Secrets tab
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Database type: 'memory' for development or 'pg'/'postgres' for PostgreSQL
# If not set, defaults to 'memory' when DATABASE_URL is missing
DB_TYPE=pg

# ================================================
# MYSQL PARENT APP CONNECTION (Optional)
# ================================================
# Read-only access to parent ERP application database
# Only needed if your microservice needs to read parent app data
# (locations, rooms, users, etc.)
MYSQL_HOST=your_mysql_host
MYSQL_USER=your_mysql_user
MYSQL_PASSWORD=your_mysql_password
MYSQL_PORT=3306
MYSQL_DATABASE=your_mysql_database
MYSQL_DEBUG=false

# ================================================
# AI SERVICE KEYS (Optional)
# ================================================
# Only add these if your microservice uses AI features
# Get these from the respective AI service providers
OPENAI_API_KEY=sk-...your-openai-key...
ANTHROPIC_API_KEY=sk-ant-...your-anthropic-key...
PERPLEXITY_API_KEY=pplx-...your-perplexity-key...

# ================================================
# AWS S3 CONFIGURATION (Optional)
# ================================================
# Only needed if your microservice handles file uploads
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=us-east-1
S3_BUCKET_NAME=your_bucket_name

# ================================================
# APPLICATION SETTINGS
# ================================================
# Environment: 'development' or 'production'
NODE_ENV=development

# Port for the application (Replit always uses 5000)
PORT=5000

# Enable development-only features like TokenSwitcher
# Set to 'false' or remove in production
ENABLE_DEV_TOOLS=true